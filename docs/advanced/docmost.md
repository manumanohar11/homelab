# Advanced Docmost Publishing

[Back to README](../../README.md)

Docmost publishing is maintainer-only.

The generated import bundle now lives under `build/docmost-space/`, not under `docs/`.

## Build

```bash
make docs-build
```

## Validate

```bash
python3 scripts/build-docmost-space.py --check
```
