# ~/media-stack/docker-compose.requests.yml
# Contains media request services (Overseerr, Jellyseerr).
# Note: These are most useful when the `arr` profile is enabled.

x-common-env: &common-env
  PUID: ${PUID}
  PGID: ${PGID}
  TZ: ${TZ}

x-service-defaults: &service-defaults
  restart: unless-stopped
  security_opt:
    - no-new-privileges:true
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"

x-healthcheck-defaults: &healthcheck-defaults
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 30s

x-resources-small: &resources-small
  deploy:
    resources:
      limits:
        cpus: '0.5'
        memory: 256M
      reservations:
        memory: 64M

x-resources-medium: &resources-medium
  deploy:
    resources:
      limits:
        cpus: '1'
        memory: 512M
      reservations:
        memory: 128M

services:
  # Overseerr - Plex request platform (requires *arr stack)
  overseerr:
    <<: [*service-defaults, *resources-medium]
    image: lscr.io/linuxserver/overseerr:latest
    container_name: overseerr
    environment:
      <<: *common-env
    volumes:
      - ${DOCKER_BASE_DIR}/overseerr:/config
    networks:
      - media-stack
    ports:
      - "5055:5055"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5055/api/v1/status"]
      <<: *healthcheck-defaults
      start_period: 30s
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    profiles:
      - requests

  # Jellyseerr - Jellyfin request platform (requires *arr stack)
  jellyseerr:
    <<: [*service-defaults, *resources-medium]
    image: fallenbagel/jellyseerr:latest
    container_name: jellyseerr
    environment:
      TZ: ${TZ}
      LOG_LEVEL: info
    volumes:
      - ${DOCKER_BASE_DIR}/jellyseerr:/app/config
    networks:
      - media-stack
    ports:
      - "5056:5055"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5055/api/v1/status"]
      <<: *healthcheck-defaults
      start_period: 30s
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    profiles:
      - jellyfin

  # Notifiarr for unified notifications across *Arr apps
  notifiarr:
    <<: [*service-defaults, *resources-small]
    image: golift/notifiarr:latest
    container_name: notifiarr
    hostname: notifiarr
    environment:
      TZ: ${TZ}
      DN_API_KEY: ${NOTIFIARR_API_KEY:-}
    volumes:
      - ${DOCKER_BASE_DIR}/notifiarr:/config
      - /var/run/utmp:/var/run/utmp:ro
    networks:
      - media-stack
    ports:
      - "5454:5454"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5454"]
      <<: *healthcheck-defaults
      start_period: 30s
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    profiles:
      - notifiarr
